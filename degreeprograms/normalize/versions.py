"""Version stamps for the normalization layer.

Bump ``NORMALIZER_VERSION`` when normalization logic changes so that outputs can
be selectively reprocessed (Phase 24 in the spec).
"""

NORMALIZER_VERSION = "0.1.0"
NORMALIZE_SCHEMA_VERSION = "0.1.0"
